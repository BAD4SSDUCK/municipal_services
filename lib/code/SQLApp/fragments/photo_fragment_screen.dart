import 'package:flutter/material.dart';

class PhotoFragmentScreen extends StatelessWidget{
//
  @override
  Widget build(BuildContext context){
    return Scaffold(
      body: Center(
        child: Text(
            "Photo Fragment Screen."
        ),
      ),
    );
  }
}